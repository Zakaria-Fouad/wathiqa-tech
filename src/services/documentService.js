// Document API service
export const documentService = {
  generate: async (type, data) => {
    // call backend API
  },
  getAll: async () => {
    // call backend API
  },
  remove: async (id) => {
    // call backend API
  },
};
