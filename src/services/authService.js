// Auth API service
export const authService = {
  login: async (email, password) => {
    // call backend API
  },
  register: async (data) => {
    // call backend API
  },
  logout: async () => {
    // call backend API
  },
};
